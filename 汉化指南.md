# Jenkins 简体中文汉化与构建指南

---

## 一、项目工作原理

本插件**几乎不含 Java 代码**，核心就是「一大堆 `*_zh_CN.properties` 文本文件」。

- 入口类 [src/main/java/io/jenkins/plugins/localization_zh_cn/LocalizationContributorImpl.java](src/main/java/io/jenkins/plugins/localization_zh_cn/LocalizationContributorImpl.java) 只注册了一个资源加载器。
- 真正干活的是依赖 [localization-support](https://github.com/jenkinsci/localization-support-plugin) 框架，它把本插件打包的中文资源塞进 Jenkins 的资源查找链，覆盖英文文案。
- [pom.xml](pom.xml) 的 `<build><resources>` 把下列三类目录的资源全部打进最终的 `.hpi`：
  - `core/src/main/resources` —— Jenkins 核心翻译
  - `plugins/*/src/main/resources` —— 各插件翻译
  - `src/main/resources` —— 本插件自身资源

**汉化工作 99% 是编辑 `.properties` 文本文件，不需要写 Java。**

---

## 二、汉化逻辑：Key 必须一一对应

### 1. `.properties` 文件格式

```
Key=中文文案
```

例（[plugins/ssh-slaves-plugin/.../Messages_zh_CN.properties](plugins/ssh-slaves-plugin/src/main/resources/hudson/plugins/sshslaves/Messages_zh_CN.properties)）：

```properties
SSHLauncher.CopyingAgentJar={0} [SSH] \u6B63\u5728\u62F7\u8D1D\u6700\u65B0\u7248\u672C\u7684 remoting.jar...
SSHLauncher.ConnectionClosed={0} [SSH] \u8FDE\u63A5\u5173\u95ED\u3002
```

其中 `\u6B63\u5728` 是「正在」的 ASCII 转义。`{0}` 是占位符，原样保留。

### 2. Key 必须和原插件英文源完全一致

`.properties` 里的 `Key` 必须与**原插件（英文源）的 `Messages.properties` 中的 Key 完全一致**，否则不会生效。

| 翻译对象 | 英文源位置 |
|---|---|
| Jenkins 核心文案 | https://github.com/jenkinsci/jenkins 的 `core/src/main/resources/` |
| 各插件文案 | 对应插件仓库的 `src/main/resources/<包路径>/Messages.properties` |
| 页面 Jelly 模板 | 对应 `*_zh_CN.properties`、`index_zh_CN.properties` 等 |

### 3. 目录路径必须对应原插件包名

本项目中翻译文件的目录结构要**照搬**原插件的 resources 目录结构。

例：SSH Slaves 插件英文源在 `hudson/plugins/sshslaves/Messages.properties`，本项目对应翻译文件就放在：

```
plugins/ssh-slaves-plugin/src/main/resources/hudson/plugins/sshslaves/Messages_zh_CN.properties
```

### 4. 中文编码方式

`.properties` 规范是 ISO-8859-1，中文需要转成 `\uXXXX` 转义。本项目所有现有文件都遵循此约定。

三种处理方式（任选其一）：

- **IntelliJ IDEA**（推荐）：装 *Resource Bundle Editor* 插件，直接写中文，保存时自动转义。
- **在线工具**：https://native2ascii.net/ 手动转换。
- **命令行**：JDK 自带 `native2ascii`（旧版）或 `mvn` 构建时自动处理（需配置）。

> 现代版 Jenkins 其实也能读 UTF-8 的 properties，但**跟随项目现有约定用 ASCII 转义最稳妥**，避免乱码。

---

## 三、构建环境要求

### 1. 软件依赖

| 组件 | 版本 | 说明 |
|---|---|---|
| JDK | **17** | Jenkins 2.578 要求 Java 17，构建也用 17 |
| Maven | **3.9+** | 父 POM `plugin:4.72` 要求 |
| IntelliJ IDEA Community | 最新版 | 可选，但强烈推荐（免费、对 properties 支持好） |
| Git | 任意 | 拉取源码、版本管理 |

### 2. JDK 17 安装与验证

```powershell
# 验证 Java 版本
java -version
# 应输出类似: openjdk version "17.0.x"

# 验证 Maven
mvn -version
# 应输出 Maven 3.9.x，并识别到 JDK 17
```

### 3. Maven 配置（可选但推荐）

在 `~/.m2/settings.xml` 中加入 Jenkins 仓库镜像，加速依赖下载：

```xml
<settings>
  <mirrors>
    <mirror>
      <id>jenkins-public</id>
      <mirrorOf>repo.jenkins-ci.org</mirrorOf>
      <url>https://repo.jenkins-ci.org/public/</url>
    </mirror>
  </mirrors>
</settings>
```

项目 [pom.xml](pom.xml) 已经声明了 Jenkins 仓库，但国内访问可能较慢，建议配合代理或镜像使用。

---

## 四、构建方法

### 1. 首次构建（验证环境）

在项目根目录执行：

```powershell
mvn clean package -DskipTests
```

或使用项目自带的 [Makefile](Makefile)（需安装 make，Windows 可用 Git Bash / WSL）：

```bash
make package
```

构建成功后产物位于：

```
target/localization-zh-cn.hpi
```

### 2. 安装到 Jenkins

1. 打开 Jenkins → **Manage Jenkins** → **Plugins**。
2. 点击右上角齿轮 → **Advanced settings**（或直接进入 **Deploy Plugin**）。
3. 上传 `target/localization-zh-cn.hpi`。
4. 重启 Jenkins 使翻译生效。

### 3. 本地快速验证（开发循环）

每次修改翻译后：

```powershell
# 1. 重新打包
mvn clean package -DskipTests

# 2. 重新上传到 Jenkins 并重启
```

> 提示：纯 `.properties` 文件改动其实不需要完整 `clean`，可只跑 `mvn package -DskipTests` 加快速度。如果遇到资源没刷新再加 `clean`。

### 4. CLI 命令行上传（可选）

项目 README 提到可用 [jcli](https://github.com/jenkins-zh/jenkins-cli) 上传：

```bash
jcli plugin upload target/localization-zh-cn.hpi
```

适合频繁迭代。

---

## 五、JDK 版本兼容：一个 JAR 即可

**不需要为每个 JDK 版本单独构建，一个 JAR 通吃多个版本。**

### 原理

- Jenkins 插件最终是 `.hpi`（本质是 `.jar` + 资源 + 元数据）。
- 决定兼容性的是**字节码版本**（`source` / `target` / `--release`），不是构建用的 JDK 版本。
- 字节码**向前兼容**：用 Java 11 字节码编译的 JAR，能在 Java 11/17/21 上运行。
- Jenkins 插件父 POM（`plugin:4.72`）默认 `<java.version>11</java.version>`，即默认产出 Java 11 字节码。

### 配置建议

| 你的目标 Jenkins 版本 | 构建用 JDK | pom 配置 | 产物兼容性 |
|---|---|---|---|
| 仅 Jenkins 2.578（Java 17） | JDK 17 | 默认即可 | Java 17+ |
| 同时兼容 Java 11 老版本 | JDK 17 | `<java.version>11</java.version>`（父 POM 默认值） | Java 11/17/21 |

**唯一规则：构建用的 JDK 版本 ≥ 字节码目标版本。**

用 JDK 17 编译、target=11，产物可在 11/17/21 运行。**无需多版本构建矩阵。**

---

## 六、针对 Jenkins 2.578 的两个关键注意点

### 1. 升级 pom 中的 Jenkins 版本

[pom.xml:24](pom.xml#L24) 当前声明：

```xml
<jenkins.version>2.387.3</jenkins.version>
```

建议改为你实际的 Jenkins 版本（如 `2.578.x` 或对应 LTS 版本号），让构建期 API 与运行时一致：

```xml
<jenkins.version>2.578.1</jenkins.version>
```

> 对纯资源类插件影响不大，但能避免潜在的 API 不匹配问题。

### 2. ⚠️ 最重要：Key 可能对不上

本项目 `plugins/` 下打包的翻译对应的是**多年前的插件版本**。Jenkins 2.578 自带的插件版本更新，英文 `Messages.properties` 的 Key 可能已**增、删、改**，导致你的中文 Key 不匹配、不生效。

**汉化前必须核对 Key**：

1. 在 Jenkins 里 **Manage Jenkins → Plugins** 查看每个插件的实际版本号。
2. 到对应插件的 GitHub 仓库，切换到对应版本 tag，查看英文 `Messages.properties` 的 Key。
3. 与本项目 `plugins/<对应插件>/.../Messages_zh_CN.properties` 中的 Key 对比。
4. 缺失的补上，删除的清理掉，改名（罕见但可能）的同步修改。

---

## 七、实战流程示例

以 SSH Slaves 插件某条文案汉化为例：

### Step 1：发现未翻译文案

在 Jenkins 节点配置页面看到「The selected credentials cannot be found」是英文。

### Step 2：定位英文源 Key

1. 查 Jenkins 里 SSH Slaves 插件版本，假设是 `2.1004.v6ed61d9f1d02`。
2. 到 https://github.com/jenkinsci/ssh-slaves-plugin 切到该 tag。
3. 在 `src/main/resources/hudson/plugins/sshslaves/Messages.properties` 找到：

```properties
SSHLauncher.SelectedCredentialsMissing=The selected credentials cannot be found
```

### Step 3：核对/添加翻译

查看本项目 [plugins/ssh-slaves-plugin/src/main/resources/hudson/plugins/sshslaves/Messages_zh_CN.properties](plugins/ssh-slaves-plugin/src/main/resources/hudson/plugins/sshslaves/Messages_zh_CN.properties#L32)：

```properties
SSHLauncher.SelectedCredentialsMissing=The selected credentials cannot be found
```

发现这条根本没翻译（还是英文）。改为：

```properties
SSHLauncher.SelectedCredentialsMissing=\u627E\u4E0D\u5230\u6240\u9009\u7684\u51ED\u636E
```

（`\u627E\u4E0D\u5230\u6240\u9009\u7684\u51ED\u636E` = 「找不到所选的凭据」）

### Step 4：重新构建

```powershell
mvn package -DskipTests
```

### Step 5：上传验证

上传 `target/localization-zh-cn.hpi` 到 Jenkins，重启，刷新节点配置页面确认生效。

---

## 八、目录结构速查

```
localization-zh-cn-plugin/
├── pom.xml                           # Maven 构建配置
├── Makefile                          # 简化构建命令
├── core/
│   └── src/main/resources/           # Jenkins 核心翻译（对应 jenkinsci/jenkins 仓库）
├── plugins/
│   ├── ssh-slaves-plugin/
│   │   └── src/main/resources/
│   │       └── hudson/plugins/sshslaves/Messages_zh_CN.properties
│   ├── git-plugin/...
│   ├── credentials-plugin/...
│   └── ... (共 40+ 插件目录)
└── src/
    └── main/
        ├── java/                     # 本插件自身的 Java 代码（很少）
        └── resources/                # 本插件自身资源
```

新增插件翻译时，在 `plugins/` 下新建目录，**目录名随意但建议加 `-plugin` 后缀**，内部 resources 路径必须严格对应原插件包名。

---

## 九、常见问题

### Q1：翻译后不生效？
- 检查 Key 是否与原插件英文源完全一致（大小写、拼写）。
- 检查文件路径是否对应原插件包路径。
- 确认插件版本与翻译 Key 匹配（见第六节）。
- 确认 Jenkins 重启过。
- 确认 Jenkins 的 Locale 设置为简体中文（装 [locale-plugin](https://github.com/jenkinsci/locale-plugin)）。

### Q2：中文乱码？
- 确认 `.properties` 文件用 ASCII 转义（`\uXXXX`），不要直接写 UTF-8 中文（除非确认运行环境支持）。
- 检查编辑器是否无意中改了文件编码。

### Q3：构建报错 `Could not resolve dependencies`？
- 检查网络能否访问 `https://repo.jenkins-ci.org/public/`。
- 配置 Maven 镜像（见第三节）。
- 尝试 `mvn clean install -U` 强制更新依赖。

### Q4：要不要改 Java 代码？
- **不需要**。除非要新增插件资源目录（需同步在 [pom.xml](pom.xml) 的 `<build><resources>` 里加一条 `<resource>` 声明）。

### Q5：如何批量对比本项目 Key 与目标插件版本的 Key 差异？
可以用脚本对比两个 `Messages.properties` 的 Key 集合。例如用 PowerShell：

```powershell
$existing = Get-Content "本项目 Messages_zh_CN.properties" | ForEach-Object { ($_ -split '=')[0] } | Where-Object { $_ -and -not $_.StartsWith('#') }
$upstream = Get-Content "上游 Messages.properties" | ForEach-Object { ($_ -split '=')[0] } | Where-Object { $_ -and -not $_.StartsWith('#') }
# 上游有但本项目缺的
Compare-Object $existing $upstream
```

---

## 说明

此文档是大模型给出的内容，没有修改过，具体内容需要自行辨别！

此文档是大模型给出的内容，没有修改过，具体内容需要自行辨别！

此文档是大模型给出的内容，没有修改过，具体内容需要自行辨别！
